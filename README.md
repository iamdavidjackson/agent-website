# iamdavidjackson

This repository contains David Jackson's public portfolio, shared career
content, and private application tooling.

```text
content/         Shared Markdown evidence and job context
website/         Public portfolio and recruiter-facing chat
career-studio/   Private job research and application workspace
```

Each application has its own package, Cloudflare configuration, and deployment
lifecycle. The Markdown under `content/` is the canonical source used by both
applications.
