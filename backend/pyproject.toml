[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3",
    "langchain-anthropic>=0.3",
    "langchain-community>=0.3",
    "langgraph>=0.2",
    "python-frontmatter>=1.0",
    "rank-bm25>=0.2",
]
